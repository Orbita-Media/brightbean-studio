---
phase: 01-orbita-social-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/base.html
  - templates/account/email/*.html
  - templates/**/*.html
  - theme/static/img/logo.svg
  - theme/templates/theme/meta.html
  - (Coolify ENV: APP_URL, ALLOWED_HOSTS, EMAIL_HOST_USER, EMAIL_HOST_PASSWORD)
  - (Cloudflare DNS: social.orbita-media.de A-Record)
  - (Cloudflare WAF: Rule für /django-admin/*)
  - (Dashboard-Repo: src/app/page.tsx oder src/components/ToolGrid)
autonomous: false
requirements:
  - REBRAND-01
  - DOMAIN-01
  - DOMAIN-02
  - DOMAIN-03
  - DOMAIN-04
  - DOMAIN-05
  - SEC-01
  - SEC-02
  - SEC-03
  - DASH-01
  - DASH-02
  - ADMIN-01
  - INVITE-01
  - MAIL-01
  - CLEAN-01
user_setup:
  - service: "Amazon SES (eu-north-1)"
    why: "SMTP-Credentials für EMAIL_HOST_USER + EMAIL_HOST_PASSWORD (Invite-Mails)"
    env_vars:
      - name: EMAIL_HOST_USER
        source: "AWS Console → IAM → SES SMTP Credentials → Create SMTP credentials → SMTP username"
      - name: EMAIL_HOST_PASSWORD
        source: "AWS Console → IAM → SES SMTP Credentials → SMTP password (nur einmal sichtbar, sofort speichern)"
    dashboard_config:
      - task: "Absender-Adresse noreply@orbita-media.de in SES verifizieren (falls nicht schon geschehen)"
        location: "AWS Console → SES eu-north-1 → Verified identities"

must_haves:
  truths:
    - "https://social.orbita-media.de/ lädt (HTTP 200) mit Orbita-Social-Branding (Logo + Titel, kein BrightBean sichtbar)"
    - "Noah kann sich mit kontakt@orbita-media.de einloggen und ist Django-Superuser"
    - "Orbita Media Dashboard zeigt eine Kachel 'Orbita Social' die auf https://social.orbita-media.de zeigt"
    - "Ein Test-Invite an eine externe E-Mail führt zu einer SES-zugestellten Invite-Mail, der Empfänger kann sich registrieren und einloggen"
    - "Direkter Aufruf von https://social.orbita-media.de/django-admin/ von einer Nicht-Tailscale-IP wird von Cloudflare WAF mit 403 geblockt"
    - "DNS brightbean.orbita-media.de liefert entweder 301-Redirect auf social.orbita-media.de oder existiert nicht mehr"
    - "Cloudflare Bot Fight Mode ist für Zone orbita-media.de aktiviert"
  artifacts:
    - path: "templates/base.html"
      provides: "Gebrandetes Layout mit 'Orbita Social'-Title, Orbita-Logo, keine BrightBean-Referenzen"
      contains: "Orbita Social"
    - path: "theme/static/img/logo.svg (oder .png)"
      provides: "Orbita-Logo statt BrightBean-Logo"
    - path: "(Coolify App xos84sccocw488o8kccow88g ENVs)"
      provides: "APP_URL=https://social.orbita-media.de, ALLOWED_HOSTS enthält social.orbita-media.de, EMAIL_HOST_USER/PASSWORD gesetzt"
    - path: "(Cloudflare DNS orbita-media.de)"
      provides: "A-Record social → 5.75.158.30 proxied, brightbean → entfernt oder 301"
    - path: "(Cloudflare WAF Rule)"
      provides: "Custom Rule 'Block Django Admin except Tailscale' für Host social.orbita-media.de"
    - path: "(Dashboard Orbita Media Tool Repo)"
      provides: "Neue Kachel 'Orbita Social' mit href=https://social.orbita-media.de"
    - path: "(Production-DB User-Tabelle)"
      provides: "User-Row kontakt@orbita-media.de mit is_superuser=true, is_staff=true"
  key_links:
    - from: "Cloudflare DNS social.orbita-media.de"
      to: "Hetzner 5.75.158.30 → Traefik → Coolify App xos84sccocw488o8kccow88g"
      via: "A-Record proxied + Traefik Label (Domain via Coolify API gesetzt)"
      pattern: "social.orbita-media.de"
    - from: "Coolify ENV APP_URL + ALLOWED_HOSTS"
      to: "Django Request-Handling"
      via: "settings/base.py"
      pattern: "ALLOWED_HOSTS.*social.orbita-media.de"
    - from: "Dashboard-Kachel"
      to: "https://social.orbita-media.de"
      via: "Next.js <a href=...> in Tool-Grid-Komponente"
      pattern: "social.orbita-media.de"
    - from: "Django-allauth Invite-Flow"
      to: "Amazon SES SMTP (email-smtp.eu-north-1.amazonaws.com:587)"
      via: "EMAIL_HOST_USER + EMAIL_HOST_PASSWORD ENV-Vars"
      pattern: "EMAIL_HOST_USER"
---

<objective>
Transformiere die bereits deployte BrightBean-Studio-Instanz (Coolify App `xos84sccocw488o8kccow88g`, aktuell erreichbar unter `https://brightbean.orbita-media.de`) in die produktive "Orbita Social"-App unter `https://social.orbita-media.de`, integriert als Kachel im Orbita Media Dashboard, mit funktionierendem Admin-Zugang und Multi-User-Invite-Flow via Amazon SES.

Purpose: Ein zentraler, team-fähiger, self-hosted Social-Media-Management-Hub für Orbita Media ohne SaaS-Gebühren, konsistent gebrandet und ins bestehende Tool-Ökosystem eingebunden.

Output:
- Rebrand-Commit im Fork `Orbita-Media/brightbean-studio` (deployed via GitHub-Webhook → Coolify)
- Neue Domain live (Cloudflare DNS + Coolify-Domain-Config + CF WAF-Regel)
- Dashboard-Kachel in `Dashboard Orbita Media Tool` commited
- Production-DB: Superuser + erfolgreich eingeladener Test-User
- CLEAN-01 erfüllt: alte Domain entfernt oder 301
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Arbeitsverzeichnisse:**
- BrightBean Fork: `C:/Users/nmdar/Nextcloud/TOOLS/BrightBean Studio/`
- Dashboard-Repo: `C:/Users/nmdar/Nextcloud/TOOLS/Dashboard Orbita Media Tool/`
- Server-Zugriff: `ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30`

**Coolify-API (nur via SSH zum Server erreichbar — `http://localhost:8000/api/v1`):**
- Token: `15|kwdeploy2026secrettoken`
- App-UUID: `xos84sccocw488o8kccow88g` (BrightBean/Orbita Social)
- App-UUID: `qok404c8oowc8gc4gwgw4kw0` (Orbita Media Dashboard — für Referenz)

**Cloudflare-API:**
- Token: `rNSCUH2Ae38e8l4B9mYJUW4t6DE8lp3ujW8zGb2m`
- Zone ID orbita-media.de: `f9c19ebd6a791cd994c9ce9c29be2db5`
- API-Base: `https://api.cloudflare.com/client/v4`

**Tailscale CGNAT-Range** (für WAF-Whitelist): `100.64.0.0/10`

**Kritisches Credential-Gap:**
- Amazon SES SMTP-User + Password fehlen noch. Task 5 (MAIL-01) ist ein **Checkpoint:human-action** — Noah muss im AWS-Console SMTP-Credentials erzeugen, bevor der Invite-Flow getestet werden kann.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Bekannte Template-Stellen (aus dem Fork-Repo verifiziert) -->
<!-- BrightBean-Templates-Struktur: -->
<!-- templates/base.html — globales Layout, enthält Title + Nav + Logo-Referenz -->
<!-- templates/account/email/*.html — django-allauth Mail-Templates (Signup, Password Reset, Invite, Verify) -->
<!-- theme/static/img/ — Logo-Assets (PNG/SVG) -->
<!-- theme/templates/theme/meta.html — OG-Tags, Favicon, SEO-Meta -->

<!-- Django settings — NICHT anfassen außer über ENV-Vars: -->
<!-- config/settings/base.py liest APP_URL, ALLOWED_HOSTS, EMAIL_HOST, EMAIL_HOST_USER, EMAIL_HOST_PASSWORD, EMAIL_PORT, EMAIL_USE_TLS aus env -->

<!-- Coolify REST-API Endpoints (auf Server: http://localhost:8000/api/v1): -->
<!-- GET    /applications/{uuid}                       → App-Info -->
<!-- GET    /applications/{uuid}/envs                   → ENV-Liste -->
<!-- POST   /applications/{uuid}/envs                   → ENV setzen {key,value,is_preview?,is_build_time?} -->
<!-- DELETE /applications/{uuid}/envs/{env_uuid}        → ENV löschen -->
<!-- POST   /applications/{uuid}/domains                → Domain hinzufügen {domain} -->
<!-- GET    /deploy?uuid={uuid}&force=true              → Deployment triggern -->

<!-- Cloudflare REST-API (auf api.cloudflare.com/client/v4): -->
<!-- GET    /zones/{zone_id}/dns_records?name=...       → Records finden -->
<!-- POST   /zones/{zone_id}/dns_records                → Record anlegen {type,name,content,proxied,ttl} -->
<!-- DELETE /zones/{zone_id}/dns_records/{rec_id}       → Record löschen -->
<!-- PATCH  /zones/{zone_id}/settings/bot_fight_mode    → Bot Fight Mode {value:"on"} -->
<!-- PATCH  /zones/{zone_id}/settings/security_level    → Security Level {value:"medium"} -->
<!-- POST   /zones/{zone_id}/rulesets/phases/http_request_firewall_custom/entrypoint → WAF-Regel -->
</interfaces>
</context>

<wave_structure>
## Ausführungs-Wellen

**Welle 1 (parallel):** Task 1 (Rebranding-Code), Task 2 (DNS + Cloudflare-Setup), Task 6 (Dashboard-Kachel)
- Alle drei Tasks haben keine Abhängigkeit voneinander
- Task 1 committet im Fork-Repo → Auto-Deploy läuft (wird in Welle 3 auf neuer Domain bestätigt)
- Task 2 setzt DNS + WAF unabhängig
- Task 6 committet im Dashboard-Repo

**Welle 2 (sequentiell nach Welle 1):** Task 3 (Coolify Domain + ENVs umstellen), Task 4 (Admin-User)
- Task 3 braucht Welle 1 fertig (sonst zeigt neue Domain "BrightBean" und/oder liefert 522)
- Task 4 (Admin-User) kann parallel zu Task 3 laufen (nur `docker exec` in laufenden Container)

**Welle 3 (Checkpoint + Verify):** Task 5 (SES-Credentials + Invite-Test), Task 7 (Smoke-Test + Cleanup CLEAN-01)
- Task 5 ist **Checkpoint** (Noah muss SES SMTP-Creds erzeugen)
- Task 7 verifiziert alle 6 Success Criteria und entfernt alten DNS-Record

**Parallelität:** Welle 1 = 3 parallel, Welle 2 = 2 parallel, Welle 3 sequenziell (Checkpoint + finaler Verify). Geschätzt ~50% Zeitersparnis ggü. seriell.
</wave_structure>

<tasks>

<task type="auto">
  <name>Task 1: Rebranding aller UI-Texte/Templates auf "Orbita Social" (REBRAND-01)</name>
  <files>
    - templates/base.html
    - templates/account/email/*.html
    - theme/templates/theme/meta.html
    - theme/static/img/logo.* (Ersatz-Asset)
    - Weitere Templates, die im grep-Scan "BrightBean"/"brightbean" matchen
  </files>
  <action>
**Arbeitsverzeichnis:** `C:/Users/nmdar/Nextcloud/TOOLS/BrightBean Studio/`

1. **Scan aller BrightBean-Vorkommen:**
   ```bash
   cd "C:/Users/nmdar/Nextcloud/TOOLS/BrightBean Studio"
   rg -l -i "brightbean" templates/ theme/ config/ apps/ --glob '!*.pyc'
   ```
   Erwartet: vor allem `templates/base.html`, `templates/account/email/*.html`, `theme/templates/theme/meta.html`, ggf. `config/settings/*.py` (nur Default-Strings, kein Code-Rename).

2. **Textersetzungen durchführen:**
   - `"BrightBean Studio"` → `"Orbita Social"`
   - `"BrightBean"` (standalone) → `"Orbita Social"`
   - `"brightbean"` (URL/Slugs in Templates, NICHT in Python-Imports) → `"orbita-social"`
   - **Wichtig**: KEINE Python-Package-Namen/Imports/Model-Namen anfassen (Out-of-Scope laut PROJECT.md — nur Präsentations-Schicht).
   - Für jeden Treffer aus Schritt 1 via Read-Tool Datei laden, via Edit-Tool Ersetzung durchführen.

3. **Logo austauschen:**
   - Orbita-Logo-Datei finden (im Dashboard-Repo unter `public/logo.svg` oder im Obsidian-Vault; sonst: Text-Logo `<span class="font-bold">Orbita Social</span>` mit Orbita-Purple `#635BFF` als Fallback).
   - Ersetze `theme/static/img/logo.svg` (oder entsprechende Datei) durch Orbita-Logo.
   - Falls BrightBean mehrere Logo-Varianten hat (light/dark/favicon): alle ersetzen. Favicon via 32x32 PNG.

4. **OG-Tags + Favicon prüfen:**
   - `theme/templates/theme/meta.html`: OG-Title/Description auf "Orbita Social" + Beschreibungstext aus PROJECT.md "Core Value" anpassen.
   - `<meta name="theme-color">` auf `#635BFF` (Orbita-Purple).

5. **Verify vor Commit:**
   ```bash
   # Keine BrightBean-Referenzen mehr in Templates:
   rg -i "brightbean" templates/ theme/ --glob '!*.pyc' || echo "CLEAN"
   ```
   Wenn noch Treffer in reinen Python-Dateien auftauchen: akzeptieren (Out of Scope). Wenn in Templates: weiter fixen.

6. **Atomarer Commit + Push:**
   ```bash
   git add templates/ theme/
   git commit -m "feat(rebrand): Orbita Social statt BrightBean in UI-Templates + Logo"
   git push origin main
   ```
   Push triggert GitHub-Webhook → Coolify-Auto-Deploy der App `xos84sccocw488o8kccow88g` (läuft ~2–3min, läuft im Hintergrund).
  </action>
  <verify>
    <automated>
      cd "C:/Users/nmdar/Nextcloud/TOOLS/BrightBean Studio" && rg -i "brightbean studio" templates/ theme/ --glob '!*.pyc' -c 2>&1 | head -5
      # Erwartet: Keine Matches (oder leer)
    </automated>
    <automated>
      # Nach ~3min: Coolify-Deployment prüfen
      ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 \
        'curl -s "http://localhost:8000/api/v1/applications/xos84sccocw488o8kccow88g" \
         -H "Authorization: Bearer 15|kwdeploy2026secrettoken" -H "Accept: application/json" \
         | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get(\"status\"))"'
      # Erwartet: "running" (nicht "exited", nicht "building")
    </automated>
    <automated>
      # Live-Test auf aktueller Domain (noch brightbean.orbita-media.de bis Task 3):
      curl -s "https://brightbean.orbita-media.de/" | grep -i "orbita social" | head -1
      # Erwartet: Match mit "Orbita Social"
    </automated>
  </verify>
  <done>
    - `rg -i brightbean templates/ theme/` liefert keine Treffer (oder nur in Kommentaren)
    - Commit in `Orbita-Media/brightbean-studio` main gepusht
    - Coolify-Deployment status = "running" nach Build
    - Live-HTML enthält "Orbita Social" im `<title>` und Nav
    - REBRAND-01 erfüllt
  </done>
</task>

<task type="auto">
  <name>Task 2: Cloudflare DNS + Bot Fight Mode + WAF-Regel (DOMAIN-04, SEC-01, SEC-02, SEC-03)</name>
  <files>
    - Cloudflare Zone orbita-media.de (Zone-ID f9c19ebd6a791cd994c9ce9c29be2db5)
    - KEINE lokalen Datei-Änderungen
  </files>
  <action>
**API-Base:** `https://api.cloudflare.com/client/v4`
**Auth-Header:** `-H "Authorization: Bearer rNSCUH2Ae38e8l4B9mYJUW4t6DE8lp3ujW8zGb2m"`
**Zone-ID:** `f9c19ebd6a791cd994c9ce9c29be2db5`

1. **Prüfen, ob social-Record schon existiert:**
   ```bash
   curl -s "https://api.cloudflare.com/client/v4/zones/f9c19ebd6a791cd994c9ce9c29be2db5/dns_records?name=social.orbita-media.de" \
     -H "Authorization: Bearer rNSCUH2Ae38e8l4B9mYJUW4t6DE8lp3ujW8zGb2m" | python -m json.tool
   ```
   Wenn vorhanden: Skip zu Schritt 3. Sonst weiter Schritt 2.

2. **A-Record anlegen (proxied):**
   ```bash
   curl -s -X POST "https://api.cloudflare.com/client/v4/zones/f9c19ebd6a791cd994c9ce9c29be2db5/dns_records" \
     -H "Authorization: Bearer rNSCUH2Ae38e8l4B9mYJUW4t6DE8lp3ujW8zGb2m" \
     -H "Content-Type: application/json" \
     -d '{"type":"A","name":"social","content":"5.75.158.30","proxied":true,"ttl":1,"comment":"Orbita Social (BrightBean Studio Fork)"}' \
     | python -m json.tool
   ```
   Erwartet: `"success": true`.

3. **Bot Fight Mode aktivieren (SEC-01):**
   ```bash
   curl -s -X PATCH "https://api.cloudflare.com/client/v4/zones/f9c19ebd6a791cd994c9ce9c29be2db5/bot_management" \
     -H "Authorization: Bearer rNSCUH2Ae38e8l4B9mYJUW4t6DE8lp3ujW8zGb2m" \
     -H "Content-Type: application/json" \
     -d '{"fight_mode": true}' | python -m json.tool
   ```
   Falls der Endpoint nicht verfügbar ist (Free Plan-Einschränkung), Fallback:
   ```bash
   curl -s -X PATCH "https://api.cloudflare.com/client/v4/zones/f9c19ebd6a791cd994c9ce9c29be2db5/settings/security_level" \
     -H "Authorization: Bearer rNSCUH2Ae38e8l4B9mYJUW4t6DE8lp3ujW8zGb2m" \
     -H "Content-Type: application/json" \
     -d '{"value":"medium"}' | python -m json.tool
   ```
   (SEC-03 wird dadurch miterfüllt — Security Level = medium.)

4. **WAF Custom-Rule anlegen (SEC-02):** Blockiere `/django-admin/*` auf `social.orbita-media.de` außer von Tailscale-CGNAT (`100.64.0.0/10`).
   ```bash
   # Zuerst bestehende Ruleset-ID für http_request_firewall_custom/entrypoint holen:
   RULESET_ID=$(curl -s "https://api.cloudflare.com/client/v4/zones/f9c19ebd6a791cd994c9ce9c29be2db5/rulesets/phases/http_request_firewall_custom/entrypoint" \
     -H "Authorization: Bearer rNSCUH2Ae38e8l4B9mYJUW4t6DE8lp3ujW8zGb2m" | python -c "import sys,json;print(json.load(sys.stdin)['result']['id'])")

   # Rule hinzufügen:
   curl -s -X PUT "https://api.cloudflare.com/client/v4/zones/f9c19ebd6a791cd994c9ce9c29be2db5/rulesets/${RULESET_ID}" \
     -H "Authorization: Bearer rNSCUH2Ae38e8l4B9mYJUW4t6DE8lp3ujW8zGb2m" \
     -H "Content-Type: application/json" \
     -d '{
       "rules": [
         {
           "action": "block",
           "expression": "(http.host eq \"social.orbita-media.de\" and starts_with(http.request.uri.path, \"/django-admin\") and not ip.src in {100.64.0.0/10})",
           "description": "Block Django Admin except Tailscale (SEC-02)",
           "enabled": true
         }
       ]
     }' | python -m json.tool
   ```
   **Achtung:** PUT ersetzt alle Rules. Wenn Rules existieren, erst GET + Merge:
   ```bash
   curl -s "https://api.cloudflare.com/client/v4/zones/f9c19ebd6a791cd994c9ce9c29be2db5/rulesets/${RULESET_ID}" \
     -H "Authorization: Bearer rNSCUH2Ae38e8l4B9mYJUW4t6DE8lp3ujW8zGb2m" | python -m json.tool > /tmp/ruleset.json
   # Bestehende rules[] + neue Rule mergen, dann PUT.
   ```

5. **DNS-Propagation prüfen (kurz):**
   ```bash
   nslookup social.orbita-media.de 1.1.1.1
   # Erwartet: IP einer Cloudflare-Edge-Range (104.x / 172.x)
   ```

**Rollback-Notes:** DNS-Record kann via DELETE-API wieder entfernt werden. WAF-Regel ebenfalls via PUT (ohne die Rule). Cloudflare-Änderungen sind innerhalb 1-2min reversibel.
  </action>
  <verify>
    <automated>
      # DNS-Record existiert und ist proxied:
      curl -s "https://api.cloudflare.com/client/v4/zones/f9c19ebd6a791cd994c9ce9c29be2db5/dns_records?name=social.orbita-media.de" \
        -H "Authorization: Bearer rNSCUH2Ae38e8l4B9mYJUW4t6DE8lp3ujW8zGb2m" | grep -E '"proxied":\s*true' && echo OK
    </automated>
    <automated>
      # Security Level = medium:
      curl -s "https://api.cloudflare.com/client/v4/zones/f9c19ebd6a791cd994c9ce9c29be2db5/settings/security_level" \
        -H "Authorization: Bearer rNSCUH2Ae38e8l4B9mYJUW4t6DE8lp3ujW8zGb2m" | grep -E '"value":\s*"medium"' && echo OK
    </automated>
    <automated>
      # WAF-Rule existiert (Pattern-Match auf Description):
      curl -s "https://api.cloudflare.com/client/v4/zones/f9c19ebd6a791cd994c9ce9c29be2db5/rulesets/phases/http_request_firewall_custom/entrypoint" \
        -H "Authorization: Bearer rNSCUH2Ae38e8l4B9mYJUW4t6DE8lp3ujW8zGb2m" | grep -i "Block Django Admin except Tailscale" && echo OK
    </automated>
  </verify>
  <done>
    - A-Record `social.orbita-media.de` → 5.75.158.30, proxied=true
    - Security Level = medium (SEC-03)
    - Bot Fight Mode versucht aktiviert (SEC-01) — Fallback medium akzeptabel
    - WAF-Regel "Block Django Admin except Tailscale" aktiv (SEC-02)
    - `nslookup social.orbita-media.de` liefert Cloudflare-Proxy-IP
  </done>
</task>

<task type="auto">
  <name>Task 3: Coolify Domain-Umstellung + ENV-Vars (DOMAIN-01, DOMAIN-02, DOMAIN-03)</name>
  <files>
    - Coolify App xos84sccocw488o8kccow88g: Domain-Config + ENV-Vars
    - KEINE Code-Änderungen
  </files>
  <action>
**Voraussetzung:** Welle 1 fertig (Rebrand-Deploy durchgelaufen, DNS für social.orbita-media.de proxied).

**Zugriff:** Alle Coolify-Calls müssen via SSH auf den Server laufen:
```bash
ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 '<curl-command>'
```

1. **Aktuelle ENVs prüfen:**
   ```bash
   ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 \
     'curl -s "http://localhost:8000/api/v1/applications/xos84sccocw488o8kccow88g/envs" \
      -H "Authorization: Bearer 15|kwdeploy2026secrettoken" -H "Accept: application/json"' \
     | python -m json.tool > /tmp/envs_before.json
   cat /tmp/envs_before.json | grep -E '"key":\s*"(APP_URL|ALLOWED_HOSTS)"'
   ```

2. **`APP_URL` setzen (DOMAIN-02):** Wenn bereits vorhanden, per DELETE + POST oder direkt updaten. Coolify-API-Pattern:
   ```bash
   ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 \
     'curl -s -X POST "http://localhost:8000/api/v1/applications/xos84sccocw488o8kccow88g/envs" \
      -H "Authorization: Bearer 15|kwdeploy2026secrettoken" \
      -H "Content-Type: application/json" \
      -H "Accept: application/json" \
      -d "{\"key\":\"APP_URL\",\"value\":\"https://social.orbita-media.de\"}"'
   ```
   Wenn 409 Conflict: ENV-UUID aus Schritt 1 extrahieren und DELETE, dann POST neu.

3. **`ALLOWED_HOSTS` setzen (DOMAIN-03):**
   ```bash
   ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 \
     'curl -s -X POST "http://localhost:8000/api/v1/applications/xos84sccocw488o8kccow88g/envs" \
      -H "Authorization: Bearer 15|kwdeploy2026secrettoken" \
      -H "Content-Type: application/json" \
      -H "Accept: application/json" \
      -d "{\"key\":\"ALLOWED_HOSTS\",\"value\":\"social.orbita-media.de,brightbean.orbita-media.de\"}"'
   ```
   Beide Domains gleichzeitig erlauben — ermöglicht sanften Übergang (brightbean.* bleibt 7 Tage als Redirect erreichbar).

4. **Coolify-Domain auf social.orbita-media.de umstellen (DOMAIN-01):**
   Coolify speichert Domains im `fqdn`-Feld der Application. Via REST:
   ```bash
   ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 \
     'curl -s -X PATCH "http://localhost:8000/api/v1/applications/xos84sccocw488o8kccow88g" \
      -H "Authorization: Bearer 15|kwdeploy2026secrettoken" \
      -H "Content-Type: application/json" \
      -H "Accept: application/json" \
      -d "{\"fqdn\":\"https://social.orbita-media.de,https://brightbean.orbita-media.de\"}"'
   ```
   (Beide Domains als komma-separierte Liste — Traefik-Labels werden so erzeugt, dass beide Hosts auf denselben Container routen.)

5. **Deployment mit `force=true` triggern, damit Traefik-Labels neu gerendert werden:**
   ```bash
   ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 \
     'curl -s "http://localhost:8000/api/v1/deploy?uuid=xos84sccocw488o8kccow88g&force=true" \
      -H "Authorization: Bearer 15|kwdeploy2026secrettoken" -H "Accept: application/json"'
   ```

6. **Warten bis Deployment-Status "running":**
   ```bash
   # Polling (max 5min):
   for i in $(seq 1 30); do
     STATUS=$(ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 \
       'curl -s "http://localhost:8000/api/v1/applications/xos84sccocw488o8kccow88g" \
        -H "Authorization: Bearer 15|kwdeploy2026secrettoken" -H "Accept: application/json"' \
       | python -c "import sys,json;print(json.load(sys.stdin).get('status',''))")
     echo "[$i] Status: $STATUS"
     [[ "$STATUS" == "running"* ]] && break
     sleep 10
   done
   ```

**Rollback-Notes:**
- Wenn `social.orbita-media.de` nach dem Deploy 502/522 liefert: `fqdn` via PATCH zurück auf nur `https://brightbean.orbita-media.de`, redeploy. DNS-Record kann bleiben.
- ENV-Änderungen sind reversibel (DELETE + POST mit alten Werten).
  </action>
  <verify>
    <automated>
      # APP_URL ENV gesetzt:
      ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 \
        'curl -s "http://localhost:8000/api/v1/applications/xos84sccocw488o8kccow88g/envs" \
         -H "Authorization: Bearer 15|kwdeploy2026secrettoken" -H "Accept: application/json"' \
        | grep -E '"value":\s*"https://social.orbita-media.de"' && echo OK_APP_URL
    </automated>
    <automated>
      # Live-HTTPS-Test auf neuer Domain:
      curl -s -o /dev/null -w "%{http_code}\n" "https://social.orbita-media.de/health/"
      # Erwartet: 200
    </automated>
    <automated>
      # Live-HTML enthält Orbita Social:
      curl -s "https://social.orbita-media.de/" | grep -i -E "(orbita social|Orbita Social)" | head -1
    </automated>
  </verify>
  <done>
    - `curl https://social.orbita-media.de/health/` → 200
    - `APP_URL` ENV = `https://social.orbita-media.de`
    - `ALLOWED_HOSTS` ENV enthält `social.orbita-media.de`
    - Coolify-Container `status` = running
    - HTML der Startseite zeigt "Orbita Social"
  </done>
</task>

<task type="auto">
  <name>Task 4: Django-Superuser für Noah anlegen (ADMIN-01)</name>
  <files>
    - Production-DB (via docker exec im app-Container der Coolify-App)
    - KEINE Code-Änderungen
  </files>
  <action>
**Arbeitsverzeichnis:** Hetzner-Server via SSH.

1. **App-Container-Namen finden:**
   ```bash
   ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 \
     'docker ps --filter "label=coolify.applicationId=15" --format "{{.Names}}"'
   # Alternativ:
   ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 \
     'docker ps | grep xos84sccocw488o8kccow88g | grep -v worker | grep -v migrate | awk "{print \$NF}"'
   ```
   Erwartet: ein Container-Name wie `app-xos84sccocw488o8kccow88g-...`.

2. **Check, ob User schon existiert:**
   ```bash
   CONTAINER=$(ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 \
     'docker ps --format "{{.Names}}" | grep xos84sccocw488o8kccow88g | grep -vE "worker|migrate|maintenance|postgres" | head -1')

   ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 \
     "docker exec $CONTAINER python manage.py shell -c \"
from django.contrib.auth import get_user_model
U = get_user_model()
u = U.objects.filter(email='kontakt@orbita-media.de').first()
print('EXISTS' if u else 'MISSING', u.is_superuser if u else '', u.is_staff if u else '')
\""
   ```

3. **Falls MISSING: User anlegen (nicht-interaktiv, Passwort via ENV injizieren):**
   Noah muss ein starkes Passwort wählen. Wenn Passwort noch nicht gesetzt:
   - Zufalls-Passwort generieren: `python -c "import secrets;print(secrets.token_urlsafe(24))"`
   - Passwort an Noah ausliefern (Output des Tasks) — Noah ändert es bei erstem Login via "Password Reset" oder im Profil.
   ```bash
   PW=$(python -c "import secrets;print(secrets.token_urlsafe(24))")
   echo "Temp-Passwort (sofort nach Login ändern): $PW"

   ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 \
     "docker exec -e DJANGO_SUPERUSER_EMAIL=kontakt@orbita-media.de \
                  -e DJANGO_SUPERUSER_PASSWORD='$PW' \
                  -e DJANGO_SUPERUSER_USERNAME=kontakt@orbita-media.de \
                  $CONTAINER python manage.py shell -c \"
from django.contrib.auth import get_user_model
from allauth.account.models import EmailAddress
import os
U = get_user_model()
email = os.environ['DJANGO_SUPERUSER_EMAIL']
pw = os.environ['DJANGO_SUPERUSER_PASSWORD']
u, created = U.objects.get_or_create(email=email, defaults={'username': email, 'is_superuser': True, 'is_staff': True})
u.is_superuser = True
u.is_staff = True
u.set_password(pw)
u.save()
EmailAddress.objects.update_or_create(user=u, email=email, defaults={'primary': True, 'verified': True})
print('Superuser ready:', u.email, 'superuser=', u.is_superuser, 'staff=', u.is_staff)
\""
   ```
   (BrightBean hat typischerweise email-basierten Login via django-allauth — deshalb username = email.)

4. **Falls EXISTS aber nicht Superuser:** Flags setzen:
   ```bash
   ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 \
     "docker exec $CONTAINER python manage.py shell -c \"
from django.contrib.auth import get_user_model
u = get_user_model().objects.get(email='kontakt@orbita-media.de')
u.is_superuser = True
u.is_staff = True
u.save()
print('Promoted:', u.email)
\""
   ```

**Rollback-Notes:** User-Flags zurücksetzen via gleichem shell-Pattern mit `is_superuser=False, is_staff=False`. Nicht destruktiv.
  </action>
  <verify>
    <automated>
      # User existiert und ist Superuser:
      CONTAINER=$(ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 'docker ps --format "{{.Names}}" | grep xos84sccocw488o8kccow88g | grep -vE "worker|migrate|maintenance|postgres" | head -1')
      ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 \
        "docker exec $CONTAINER python manage.py shell -c \"
from django.contrib.auth import get_user_model
u = get_user_model().objects.filter(email='kontakt@orbita-media.de').first()
assert u and u.is_superuser and u.is_staff, 'FAIL'
print('OK')
\""
      # Erwartet: "OK"
    </automated>
  </verify>
  <done>
    - User mit E-Mail `kontakt@orbita-media.de` existiert in Production-DB
    - `is_superuser=True`, `is_staff=True`
    - allauth-EmailAddress verified (kein "Verify your Email"-Block beim Login)
    - Temporäres Passwort an Noah ausgeliefert
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5 (Checkpoint): SES SMTP-Credentials + Invite-Test (MAIL-01, INVITE-01)</name>
  <what-built>
    Tasks 1–4 sind durch. UI ist gebrandet, Domain live, Admin-User existiert, WAF schützt /django-admin/. Fehlt nur noch: Amazon SES SMTP-Credentials, weil Django sonst keine Invite-Mail versenden kann.
  </what-built>
  <how-to-verify>
**Was Noah machen muss (1x ~5min, danach ist alles selbst-automatisierend):**

1. **SES SMTP-Credentials erzeugen:**
   - AWS-Console → Region `eu-north-1` (Stockholm) → Amazon SES → "SMTP settings" → "Create SMTP credentials"
   - IAM-User-Name z.B. `orbita-social-ses-smtp`
   - SMTP-Username + SMTP-Password kopieren (**Passwort wird nur einmal angezeigt!**)
   - Format-Check: Username beginnt mit `AKIA...` (oder `BKIA...`), Password ist base64-artig ~44 Zeichen

2. **Absender-Adresse prüfen:**
   - SES → "Verified identities" → Prüfen, ob `noreply@orbita-media.de` (oder Domain `orbita-media.de`) verifiziert ist
   - Falls nicht: Domain-Verifizierung via DKIM-Records in Cloudflare hinzufügen (SES liefert 3 CNAMEs).

3. **Credentials an Claude zurückgeben:**
   Format:
   ```
   EMAIL_HOST_USER=AKIA...
   EMAIL_HOST_PASSWORD=<44-Zeichen-Password>
   DEFAULT_FROM_EMAIL=noreply@orbita-media.de
   ```

**Was Claude danach automatisch macht (nach Freigabe):**

4. **ENV-Vars in Coolify setzen:**
   ```bash
   for KV in "EMAIL_HOST=email-smtp.eu-north-1.amazonaws.com" \
             "EMAIL_PORT=587" \
             "EMAIL_USE_TLS=True" \
             "EMAIL_HOST_USER=<Wert>" \
             "EMAIL_HOST_PASSWORD=<Wert>" \
             "DEFAULT_FROM_EMAIL=noreply@orbita-media.de"; do
     KEY="${KV%%=*}"; VAL="${KV#*=}"
     ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 \
       "curl -s -X POST 'http://localhost:8000/api/v1/applications/xos84sccocw488o8kccow88g/envs' \
        -H 'Authorization: Bearer 15|kwdeploy2026secrettoken' \
        -H 'Content-Type: application/json' -H 'Accept: application/json' \
        -d '{\"key\":\"$KEY\",\"value\":\"$VAL\"}'"
   done
   ```

5. **Redeploy triggern:**
   ```bash
   ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 \
     "curl -s 'http://localhost:8000/api/v1/deploy?uuid=xos84sccocw488o8kccow88g&force=true' \
      -H 'Authorization: Bearer 15|kwdeploy2026secrettoken' -H 'Accept: application/json'"
   ```

6. **Django shell: Test-Mail versenden:**
   ```bash
   docker exec <container> python manage.py shell -c "
from django.core.mail import send_mail
send_mail('Orbita Social - Test', 'SES funktioniert.', 'noreply@orbita-media.de', ['kontakt@orbita-media.de'])
print('sent')
"
   ```
   Wartet auf SES-Zustellung in Inbox von Noah.

7. **Invite-Flow UI-Test (INVITE-01):**
   - Login als Superuser auf `https://social.orbita-media.de/`
   - `/members/invite/` (oder entsprechender Pfad — BrightBean hat `apps/members/`) → Invite an `test+social@orbita-media.de` senden
   - Postfach prüfen (Noah hat Alias): Invite-Link klicken
   - Registrierung → neuer User kann einloggen
  </how-to-verify>
  <resume-signal>
    Antworte mit:
    - `approved: <EMAIL_HOST_USER>;<EMAIL_HOST_PASSWORD>` — Claude setzt ENVs und testet
    - `approved: skip-mail` — MAIL-01 + INVITE-01 bleiben offen (als Blocker in STATE.md eingetragen), Plan geht trotzdem zu Task 6/7 weiter
    - Issue-Text — wenn etwas im AWS-Setup nicht klappt, wird korrigiert
  </resume-signal>
</task>

<task type="auto">
  <name>Task 6: Orbita-Social-Kachel im Dashboard-Repo (DASH-01, DASH-02)</name>
  <files>
    - C:/Users/nmdar/Nextcloud/TOOLS/Dashboard Orbita Media Tool/src/app/ (Tool-Grid / page.tsx)
    - oder src/components/ToolGrid/ — tatsächliche Stelle ist vor Edit via Glob+Read zu finden
  </files>
  <action>
**Arbeitsverzeichnis:** `C:/Users/nmdar/Nextcloud/TOOLS/Dashboard Orbita Media Tool/`

1. **Struktur erkunden:**
   ```bash
   cd "C:/Users/nmdar/Nextcloud/TOOLS/Dashboard Orbita Media Tool"
   ls src/app/ src/components/ 2>/dev/null
   # Und grep nach existierender Tool-Kachel (z.B. "Cover Tool" oder "Keyword"):
   rg -l "Cover Tool|Keyword Tool" src/ --glob '!node_modules' 2>/dev/null
   ```

2. **Kachel-Datenstruktur finden:** Typisch ein Array wie:
   ```ts
   const tools = [
     { name: "Cover Tool", href: "https://cover.orbita-media.de", icon: ..., color: "..." },
     ...
   ];
   ```
   Datei via Read laden, neue Zeile via Edit hinzufügen:
   ```ts
   { name: "Orbita Social", href: "https://social.orbita-media.de",
     description: "Social-Media-Posts für 10 Plattformen",
     icon: <Share2Icon />,  // oder passendes Lucide-Icon (MessageSquare, Megaphone, Globe)
     color: "#635BFF" },     // Orbita-Purple
   ```
   Konsistent mit bestehenden Tool-Kacheln (gleicher Stil — prüfen durch Read eines bestehenden Eintrags).

3. **Icon wählen:** Aus Lucide/Heroicons — `Megaphone`, `Share2`, oder `MessageSquare`. Kein Custom-Asset nötig.

4. **Build prüfen (lokal):**
   ```bash
   cd "C:/Users/nmdar/Nextcloud/TOOLS/Dashboard Orbita Media Tool"
   npm run build 2>&1 | tail -20
   # Erwartet: kein TypeScript-Fehler, "Compiled successfully"
   ```
   (Falls lokal kein Node verfügbar: Commit + Push ohne lokalen Build — Coolify-Build ist der eigentliche Gate.)

5. **Commit + Push:**
   ```bash
   git add src/
   git commit -m "feat(dashboard): Orbita-Social-Kachel hinzugefügt (→ https://social.orbita-media.de)"
   git push origin main
   ```
   Triggert Coolify-Auto-Deploy der Dashboard-App `qok404c8oowc8gc4gwgw4kw0`.

6. **Dashboard-Deploy abwarten:**
   ```bash
   ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 \
     'curl -s "http://localhost:8000/api/v1/applications/qok404c8oowc8gc4gwgw4kw0" \
      -H "Authorization: Bearer 15|kwdeploy2026secrettoken" -H "Accept: application/json"' \
     | python -c "import sys,json;d=json.load(sys.stdin);print(d.get('status'))"
   ```
  </action>
  <verify>
    <automated>
      # Dashboard antwortet mit Orbita-Social-Link:
      curl -s "https://dashboard-orbita.taila9251c.ts.net/" | grep -iE 'social.orbita-media.de|Orbita Social' | head -3
      # Erwartet: mindestens ein Match
    </automated>
    <automated>
      # Dashboard-Container running:
      ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 \
        'curl -s "http://localhost:8000/api/v1/applications/qok404c8oowc8gc4gwgw4kw0" \
         -H "Authorization: Bearer 15|kwdeploy2026secrettoken" -H "Accept: application/json"' \
        | grep -o '"status":"[^"]*"' | head -1
    </automated>
  </verify>
  <done>
    - Commit im Dashboard-Repo gepusht
    - Coolify-Build der Dashboard-App erfolgreich ("running")
    - Dashboard-HTML enthält `social.orbita-media.de` und Label "Orbita Social"
    - Optisch konsistent mit anderen Tool-Kacheln (gleiches Grid/Card-Layout)
  </done>
</task>

<task type="auto">
  <name>Task 7: End-to-End Smoke-Test + CLEAN-01 (alte Domain entfernen/redirecten)</name>
  <files>
    - Cloudflare DNS (brightbean.orbita-media.de)
    - KEINE Code-Änderungen
  </files>
  <action>
**Voraussetzung:** Tasks 1–6 abgeschlossen.

### Teil A — Vollständiger Success-Criteria-Smoke-Test

1. **SC-1: Branding**
   ```bash
   curl -s "https://social.orbita-media.de/" | grep -iE "(orbita social)" | head -2
   curl -s "https://social.orbita-media.de/" | grep -iE "brightbean" | head -2
   # Erwartet: Orbita-Social matcht, brightbean matcht NICHT (leer)
   ```

2. **SC-2: Admin-Login** — Manuell:
   - Browser → `https://social.orbita-media.de/accounts/login/`
   - `kontakt@orbita-media.de` + Temp-Passwort → Login OK
   - Auf Tailscale-verbundenem Gerät → `/django-admin/` → Django-Admin lädt

3. **SC-3: Invite-Flow** — wenn Task 5 approved (sonst skip und in STATE.md als offen eintragen):
   - Eingeloggt als Admin → Invite schicken an `test+social@orbita-media.de`
   - Mail-Inbox → Link klicken → Registrierung → Login OK

4. **SC-4: Dashboard-Kachel**
   ```bash
   curl -s "https://dashboard-orbita.taila9251c.ts.net/" | grep -iE 'social.orbita-media.de'
   ```

5. **SC-5: WAF-Regel blockt /django-admin/ von nicht-Tailscale-IP**
   ```bash
   # Von öffentlicher (nicht-Tailscale) IP — z.B. vom lokalen Windows-Rechner ohne Tailscale-Aktivierung:
   curl -s -o /dev/null -w "%{http_code}\n" "https://social.orbita-media.de/django-admin/"
   # Erwartet: 403 (CF-WAF-Block)

   # Gegenprobe vom Hetzner-Server (keine Tailscale-IP, aber auch keine Blockade von Edge) — direkter Traefik-Test:
   ssh -i /c/Users/nmdar/.ssh/noah_desktop_hetzner root@5.75.158.30 \
     'curl -s -o /dev/null -w "%{http_code}\n" -H "Host: social.orbita-media.de" http://127.0.0.1/django-admin/'
   # Umgeht Cloudflare, trifft Traefik direkt → 200/302 (Django-Admin Login-Page)
   ```

6. **SC-6: Alte Domain**
   ```bash
   curl -s -o /dev/null -w "%{http_code} %{redirect_url}\n" "https://brightbean.orbita-media.de/"
   # Nach Teil B unten: Erwartet 301 → social.orbita-media.de (oder timeout wenn entfernt)
   ```

### Teil B — CLEAN-01: brightbean.orbita-media.de → 301 auf social.*

Da `ALLOWED_HOSTS` beide Domains erlaubt und Coolify-fqdn beide hat, läuft brightbean.* aktuell gleichwertig. Für sauberen 301-Redirect ohne Code-Change: **Cloudflare Page Rule / Bulk Redirect / Redirect Rule**.

**Variante 1 — Cloudflare Redirect Rule (modern, empfohlen):**
```bash
RULESET_ID=$(curl -s "https://api.cloudflare.com/client/v4/zones/f9c19ebd6a791cd994c9ce9c29be2db5/rulesets/phases/http_request_dynamic_redirect/entrypoint" \
  -H "Authorization: Bearer rNSCUH2Ae38e8l4B9mYJUW4t6DE8lp3ujW8zGb2m" \
  | python -c "import sys,json;print(json.load(sys.stdin)['result']['id'])")

# Rules holen, mergen, PUT (ähnlich wie bei WAF in Task 2):
curl -s -X PUT "https://api.cloudflare.com/client/v4/zones/f9c19ebd6a791cd994c9ce9c29be2db5/rulesets/${RULESET_ID}" \
  -H "Authorization: Bearer rNSCUH2Ae38e8l4B9mYJUW4t6DE8lp3ujW8zGb2m" \
  -H "Content-Type: application/json" \
  -d '{
    "rules": [
      {
        "action": "redirect",
        "description": "Old brightbean.orbita-media.de -> social.orbita-media.de (CLEAN-01)",
        "enabled": true,
        "expression": "http.host eq \"brightbean.orbita-media.de\"",
        "action_parameters": {
          "from_value": {
            "status_code": 301,
            "target_url": {
              "expression": "concat(\"https://social.orbita-media.de\", http.request.uri.path)"
            },
            "preserve_query_string": true
          }
        }
      }
    ]
  }'
```

**Variante 2 — DNS-Record löschen (nach 7 Tagen Beobachtungszeit laut PROJECT.md):**
Aktuell NICHT ausführen — das ist für einen späteren Zeitpunkt. Nur dokumentieren:
```bash
# SPÄTER, nicht jetzt:
REC=$(curl -s "https://api.cloudflare.com/client/v4/zones/f9c19ebd6a791cd994c9ce9c29be2db5/dns_records?name=brightbean.orbita-media.de" \
  -H "Authorization: Bearer rNSCUH2Ae38e8l4B9mYJUW4t6DE8lp3ujW8zGb2m" \
  | python -c "import sys,json;r=json.load(sys.stdin)['result'];print(r[0]['id']) if r else print('none')")
# curl -X DELETE "https://api.cloudflare.com/client/v4/zones/.../dns_records/${REC}"
```

### Teil C — Finaler Status in STATE.md dokumentieren

Nach allen Tests: STATE.md updaten mit:
- Erfüllte Requirements abhaken
- Blocker entfernen wenn Task 5 approved (sonst: MAIL-01/INVITE-01 als open behalten)
- "Last activity"-Zeile auf heute

**Rollback-Notes:**
- Redirect-Rule via PUT ohne die Regel wieder entfernen, falls brightbean.* doch direkt erreichbar bleiben soll.
- DNS-Delete ist irreversibel (braucht Neuanlage) — deshalb NICHT in diesem Plan-Schritt, sondern später nach Beobachtungsperiode.
  </action>
  <verify>
    <automated>
      # SC-1:
      curl -s "https://social.orbita-media.de/" | grep -qi "orbita social" && echo "SC-1 OK"
      curl -s "https://social.orbita-media.de/" | grep -qi "brightbean studio" && echo "SC-1 FAIL" || echo "SC-1 BrightBean-clean"
    </automated>
    <automated>
      # SC-4:
      curl -s "https://dashboard-orbita.taila9251c.ts.net/" | grep -q "social.orbita-media.de" && echo "SC-4 OK"
    </automated>
    <automated>
      # SC-5: /django-admin/ von außen = 403
      curl -s -o /dev/null -w "%{http_code}" "https://social.orbita-media.de/django-admin/" | grep -q "403" && echo "SC-5 OK"
    </automated>
    <automated>
      # SC-6: Alte Domain liefert 301:
      curl -s -o /dev/null -w "%{http_code}" "https://brightbean.orbita-media.de/" | grep -qE "^30[12]$" && echo "SC-6 OK"
    </automated>
  </verify>
  <done>
    - Alle 6 Success Criteria aus ROADMAP.md erfüllt (oder explizit als open dokumentiert wenn Task 5 skipped)
    - CLEAN-01 durch 301-Redirect erfüllt (DNS-Delete ist späterer Ops-Task, nicht Teil dieses Plans)
    - STATE.md aktualisiert: Phase 1 = complete (oder mit Blocker-Dokumentation wenn Task 5 skipped)
  </done>
</task>

</tasks>

<verification>
## Verifikations-Matrix: Requirements → Tasks → Tests

| Requirement | Task | Automated Test |
|---|---|---|
| REBRAND-01 | Task 1 | `rg -i 'brightbean' templates/` leer; HTML-Live-Test "Orbita Social" matcht |
| DOMAIN-01 | Task 3 | Coolify-API: App-fqdn enthält `https://social.orbita-media.de`; Live 200 |
| DOMAIN-02 | Task 3 | ENV APP_URL = `https://social.orbita-media.de` |
| DOMAIN-03 | Task 3 | ENV ALLOWED_HOSTS enthält `social.orbita-media.de` |
| DOMAIN-04 | Task 2 | CF DNS API: A-Record social proxied=true |
| DOMAIN-05 / CLEAN-01 | Task 7 | Redirect 301 von brightbean.* auf social.* |
| SEC-01 | Task 2 | CF Bot Fight Mode an (oder Fallback: security_level=medium) |
| SEC-02 | Task 2 + Task 7 | WAF-Regel existiert + `curl /django-admin/` = 403 von extern |
| SEC-03 | Task 2 | CF Settings security_level = medium |
| DASH-01 | Task 6 | Dashboard-HTML enthält `social.orbita-media.de` |
| DASH-02 | Task 6 | Dashboard-Kachel im gleichen Grid-Card-Layout wie Cover/Keyword |
| ADMIN-01 | Task 4 | `docker exec ... User.objects.get(email=...).is_superuser == True` |
| MAIL-01 | Task 5 (Checkpoint) | ENVs gesetzt + `send_mail()` shell-Test erfolgreich |
| INVITE-01 | Task 5 (Checkpoint) | UI-Invite → Mail → Register → Login durchgelaufen |

## Reihenfolge-Constraints (laut User-Prompt erfüllt)

1. ✅ Rebranding-Code (Task 1) pusht VOR Domain-Switch (Task 3) — Welle 1 vs Welle 2
2. ✅ DNS (Task 2) gesetzt VOR Coolify-Domain-Switch (Task 3) — Welle 1 vs Welle 2
3. ✅ Admin-User (Task 4) VOR Invite-Test (Task 5)
4. ✅ SES-Credentials (Task 5) VOR Invite-Test (im gleichen Task — Checkpoint macht Ordnung)
5. ✅ Dashboard-Kachel (Task 6) unabhängig, parallel in Welle 1

## Risiken + Mitigations

| Risiko | Mitigation |
|---|---|
| Task 3 liefert 522 auf social.* weil Rebrand noch nicht durch | Welle 1 wartet auf Rebrand-Deploy-Status "running" bevor Welle 2 startet |
| WAF-Rule-PUT überschreibt bestehende Rules | Task 2 Schritt 4 warnt + enthält GET+Merge-Pattern |
| SES-Credentials-Lieferung verzögert | Task 5 = Checkpoint mit Option "skip-mail" → INVITE/MAIL als offener Blocker in STATE.md, andere 12 Requirements können trotzdem durch |
| Dashboard-Repo hat andere Struktur als erwartet | Task 6 erkundet erst via Glob+Read, dann Edit — kein Vorabwissen angenommen |
| Cloudflare-Ruleset-PUT ohne Merge bricht bestehende Rules | Task 2 + Task 7 enthalten explizit GET→Merge→PUT-Hinweis |
</verification>

<success_criteria>
Phase 1 ist abgeschlossen wenn:

1. ✅ `https://social.orbita-media.de/` → 200, HTML zeigt "Orbita Social" (Title, Nav, Logo), keine BrightBean-Studio-Texte
2. ✅ `kontakt@orbita-media.de` kann sich einloggen, ist Superuser, `/django-admin/` via Tailscale erreichbar
3. ✅ Test-Invite an externe Mail funktioniert (Task 5 approved) — ODER als dokumentierter Blocker in STATE.md (Task 5 skipped)
4. ✅ Dashboard unter `dashboard-orbita.taila9251c.ts.net` zeigt Orbita-Social-Kachel → führt zu `https://social.orbita-media.de`
5. ✅ `curl https://social.orbita-media.de/django-admin/` von nicht-Tailscale = 403
6. ✅ `curl https://brightbean.orbita-media.de/` = 301 → `https://social.orbita-media.de`
7. ✅ Cloudflare Zone: Security Level medium, Bot Fight Mode attempted on
</success_criteria>

<output>
Nach Abschluss: Erstelle `.planning/phases/01-orbita-social-launch/01-01-SUMMARY.md` mit:
- Welche Tasks gelaufen sind + Dauer
- Live-URLs + Verifikations-Output
- Offene Punkte (z.B. wenn Task 5 skipped: MAIL-01/INVITE-01 als open)
- Empfehlung für nächste Steps (z.B. OAuth-Apps in BrightBean-Admin anlegen, Team einladen)
</output>
